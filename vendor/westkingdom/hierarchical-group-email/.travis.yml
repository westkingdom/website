language: php

php:
  - 5.5
  - 5.6
  - 7.0

before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction --prefer-dist --dev

script: phpunit --bootstrap vendor/autoload.php tests
