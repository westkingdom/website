machine:
  php:
    version: 5.5.11
  hosts:
    localhost: 127.0.0.1

test:
  pre:
    - bin/disable-php-email
    - ./install-drupal
    - ./start-webserver:
        background: true
    - ./wait-for-webserver

  override:
    - bin/behat

  post:
    - bin/stop-webserver

